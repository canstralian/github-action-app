{
  "name": "github-action-app",
  "version": "0.0.0",
  "description": "GitHub Actions application tooling and workflows",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "test": "echo 'No tests configured' && exit 0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "semantic-release": "^24.0.0"
  },
  "release": {
    "branches": ["main"]
  }
}
